version: 0.2

phases:
    build:
      commands:
        - ls
        # - docker-compose up -d
        # - export CI=true
        # - gem install coveralls-lcov
        # - echo "hello world"
        # - bundle install
        # - ruby test.rb
        # - coveralls-lcov --repo-token $COVERALLS_REPO_TOKEN coverage/lcov.info